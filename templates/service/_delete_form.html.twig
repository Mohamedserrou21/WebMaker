<form method="post" action="{{ path('app_s_e_r_v_i_c_e_delete', {'id': s_e_r_v_i_c_e.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ s_e_r_v_i_c_e.id) }}">
    <button class="btn">Delete</button>
</form>
